export default function SavingsPayments({ params }) {
  const { id } = params;

  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">Micro‑Savings Payments</h2>

      <p className="text-slate-400">Payments for Savings Plan ID: {id}</p>

      <p className="mt-6 text-slate-300">
        Payment history will appear here once backend integration is complete.
      </p>
    </div>
  );
}
