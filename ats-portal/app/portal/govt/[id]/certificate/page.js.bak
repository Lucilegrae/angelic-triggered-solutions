export default function GovernmentCertificate({ params }) {
  const { id } = params;

  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">Government Department Certificate</h2>

      <p className="text-slate-400">Certificate for Department ID: {id}</p>

      <div className="mt-6 p-6 bg-slate-900 rounded border border-slate-800">
        <p className="text-slate-300">Certificate preview will appear here.</p>
      </div>
    </div>
  );
}
