name: Supabase Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    # ATS Portal lives inside ats-portal/
    defaults:
      run:
        working-directory: ats-portal

    # Supabase CLI authentication
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1

      # Database migrations (optional but recommended)
      - name: Push database migrations
        run: supabase db push

      # Deploy ALL functions inside ats-portal/supabase/functions
      - name: Deploy all Supabase functions
        working-directory: ats-portal/supabase
        run: supabase functions deploy --project-ref "$SUPABASE_PROJECT_ID"

      # Deploy JWKS function specifically
      - name: Deploy JWKS function
        working-directory: ats-portal/supabase
        run: supabase functions deploy public-jwks --project-ref "$SUPABASE_PROJECT_ID" --no-verify-jwt
