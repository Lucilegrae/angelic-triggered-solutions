import "../theme.css";

export const metadata = {
  title: "ATS Omniversal Portal",
  description: "ATS-Infinity Sovereignty System",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <script src="/cosmicParticles.js" />
      </head>

      <body>
        <div id="cosmic-particles"></div>
        {children}
      </body>
    </html>
  );
}
