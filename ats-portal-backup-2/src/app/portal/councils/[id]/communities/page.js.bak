export default function CouncilCommunities({ params }) {
  const { id } = params;

  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">Council Communities</h2>

      <p className="text-slate-400">Communities linked to Council ID: {id}</p>

      <p className="mt-6 text-slate-300">
        Community listing will appear here once backend integration is complete.
      </p>
    </div>
  );
}
