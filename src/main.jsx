import React from "react";
import ReactDOM from "react-dom/client";
import CosmicWheel from "./components/CosmicWheel";   // import your new component

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CosmicWheel />
  </React.StrictMode>
);
